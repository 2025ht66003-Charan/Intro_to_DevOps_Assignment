apiVersion: v1
kind: Service
metadata:
  name: aceest-service
  namespace: aceest-gym
  labels:
    app: aceest-fitness-gym
spec:
  type: NodePort
  selector:
    app: aceest-fitness-gym
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
